# Publish perf report (agent)

Mirrored from [`.cursor/rules/publish-perf-report.mdc`](../../.cursor/rules/publish-perf-report.mdc).

Report publish metrics from `C35 publish perf` / `.cache/publish-perf/latest.json` including app targets (android/web/windows), `marks`, and `artifacts_bytes`. Chef base: `publish_c35_rust_chef.ps1`.
