name: alienai_c35
description: "c35 — Alien AI client (Phase 1 auth + referral)"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.3
  http: ^1.2.2
  url_launcher: ^6.3.1
  uuid: ^4.5.1
  protobuf: ^4.0.0
  fixnum: ^1.1.1
  file_picker: ^8.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
