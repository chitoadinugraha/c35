# syntax=docker/dockerfile:1.7
# Pre-baked cargo-chef for c35 cluster Rust images (publish with publish_c35_rust_chef.ps1).

FROM rust:1.89-bookworm
RUN cargo install cargo-chef --locked
WORKDIR /app
