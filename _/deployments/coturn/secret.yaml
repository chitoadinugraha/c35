# DO NOT commit real production secrets. Apply with a generated value:
#   $secret = [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Max 256 }))
#   kubectl create secret generic coturn-secret -n c35 \
#     --from-literal=static-auth-secret=$secret \
#     --dry-run=client -o yaml | kubectl apply -f -
#
# Example placeholder only (replace before cluster apply):
apiVersion: v1
kind: Secret
metadata:
  name: coturn-secret
  labels:
    app: coturn
    app.kubernetes.io/part-of: c35
type: Opaque
stringData:
  static-auth-secret: "REPLACE_WITH_openssl_rand_-base64_32"
